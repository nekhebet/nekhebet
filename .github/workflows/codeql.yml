name: CodeQL

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  schedule:
    # Еженедельный аудит даже без коммитов
    - cron: "0 3 * * 1"

jobs:
  analyze:
    name: CodeQL (Python)
    runs-on: ubuntu-latest

    permissions:
      security-events: write
      actions: read
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v4
        with:
          languages: python
          # Стандартный security-профиль
          queries: security-and-quality

      # CodeQL сам строит проект
      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v4
