# Nekhebet Omen Display

**Real-Time Event Display Pipeline (Reference Architecture)**

[![PostgreSQL](https://img.shields.io/badge/PostgreSQL-16+-336791?logo=postgresql\&logoColor=white)](https://www.postgresql.org)
[![Node.js](https://img.shields.io/badge/Node.js-18+-green?logo=node.js\&logoColor=white)](https://nodejs.org)
[![JavaScript](https://img.shields.io/badge/JavaScript-ES6+-yellow.svg)](https://developer.mozilla.org/en-US/docs/Web/JavaScript)
[![HTML5](https://img.shields.io/badge/HTML5-Valid-blue.svg)](https://validator.w3.org/)
[![CSS3](https://img.shields.io/badge/CSS3-Valid-blue.svg)](https://jigsaw.w3.org/css-validator/)
[![WebSocket](https://img.shields.io/badge/WebSocket-Realtime-green.svg)](https://developer.mozilla.org/en-US/docs/Web/API/WebSocket)

> **Nekhebet Omen Display** — это reference-проект **event-driven системы реального времени** для непрерывного визуального отображения потоковых событий (digital signage / monitoring / ambient display).
>
> Проект демонстрирует архитектурные паттерны ingest → enrich → distribute → render с упором на:
>
> * low-latency delivery,
> * автономную работу 24/7,
> * graceful degradation,
> * backpressure и отказоустойчивость на уровне UX.


<table class="mobile-table">
  <tr>
    <td colspan="2" class="mobile-main">
      <p> <img src="https://i.postimg.cc/qvRwVDvL/080808.png" alt="Nekhebet Omen Display" width="100%" /></p>
    </td>
  </tr>
  <tr>
    <td colspan="2" class="mobile-side">
    </td>
  </tr>
</table>

Система принимает события из Telegram в реальном времени, обогащает их метаданными, сохраняет в PostgreSQL и транслирует подключённым клиентам через WebSocket с учётом backpressure и состояния интерфейса.

Клиентская часть ориентирована на **непрерывное отображение**, а не на интерактивный UI в классическом смысле.

## 🚀 Быстрый старт

Клиент полностью **монолитный** — весь код, стили и логика находятся в одном файле `omen_display.html`.  
Никаких зависимостей, сборщиков или внешних библиотек.

### Развёртывание (15 секунд)
1. Скачайте или скопируйте файл `omen_display.html`.
2. Откройте файл в браузере.

### Минимальная конфигурация

```javascript
// API endpoint по умолчанию
const API_BASE = 'http://0808.us.nekhebet.su:8080';
```

## 📺 Live демо

```
http://0808.us.nekhebet.su:8080
```

## 📋 Обзор

Nekhebet Omen Display — это **верифицируемый пайплайн отображения событий в реальном времени**, предназначенный для длительной автономной работы в режиме непрерывного отображения событий. Система демонстрирует асинхронный event-driven конвейер обработки потоковых данных.

## 🏗️ Пайплайн реального времени Omen Display


```
Telegram → Python Ingest → PostgreSQL
          ↓
    ├─→ LISTEN/NOTIFY → Node.js → WebSocket → Клиенты (отображение)
    └─→ Background Worker (NER/keywords) → обратно в PostgreSQL
```

## 🎨 Архитектура отображения

1. **Text Layer** — поток сообщений в реальном времени
2. **Media Layer** — изображения, видео, анимации
3. **Word Cloud Layer** — динамическое облако слов

Все слои:

* синхронизированы по времени,
* поддерживают паузу,
* корректно деградируют при отсутствии данных.

## 🎮 Управление

<table class="mobile-table">
  <tr>
    <td colspan="2" class="mobile-main">
      <p> <img src="https://i.postimg.cc/KcPmnF6f/0808-us-nekhebet-su.png" alt="Nekhebet Omen Display" width="100%" /></p>
    </td>
  </tr>
  <tr>
    <td colspan="2" class="mobile-side">
    </td>
  </tr>
</table>

### Кнопки управления

| Элемент                       | Действие                                                                 | Визуальные состояния                              |
|-------------------------------|--------------------------------------------------------------------------|----------------------------------------------------|
| AUTO (6) 🟢<br>AUTO (0) 🟡   | Вкл/выкл автосмену медиа каждые ≈5–8 сек                    | 🟢 — активно<br>🟡 — пауза<br>цифра — секунды до смены |
| ▶ NEXT                        | Ручной переход к следующему медиа                                 | подсвечивается при срабатывании                         |
| ↻ RESET (0)                   | Полный сброс: очищает очередь, переподключает соединение | красный индикатор 6 сек<br>цифра — сообщений в очереди |

### Жесты

- **Свайп влево** по области медиа → то же, что кнопка NEXT  
- **Клик по облаку слов** → **полная пауза** (медиа + текст + анимации). Повторный клик — возобновление.

## 🔄 Типичный сценарий работы

### Первые 60 секунд

1. Запуск → показываются **выбранный текст** из файла (демо-режим, ~4 сек на фразу)  
2. Когда приходят сообщения:  
   - Выводится текст 
   - Выделяются ключевые слова
   - После текста выводится **облако слов**  
3. Каждые ≈5.5–8 сек — автоматический переход к следующему сообщению

## ⚡ Визуальная сигнализация

При поступлении сообщения или медиа с меткой приоритета включается **визуальная индикация**:

- **Двойная вспышка экрана** (случайный цвет + два быстрых fade-in/out)  
- **Временная смена радиального градиента фона** (на 3–5 секунд, разные оттенки в зависимости от метки)

## ☁️ Word Cloud Generation

Облако слов формируется **на стороне сервера** на основе последних сообщений отправителя.

Алгоритм:

1. выборка последних сообщений
2. нормализация текста
3. очистка (URL, цифры, пунктуация)
4. фильтрация стоп-слов
5. частотный анализ
6. топ-50 слов

Формат:

```json
[{ "word": "example", "freq": 12 }]
```

## 🤖 Алгоритмы анализа текста

| Алгоритм                  | Где выполняется | Что делает                                                                 | Результат передаётся клиенту в поле |
|---------------------------|-----------------|----------------------------------------------------------------------------|--------------------------------------|
| **Проверка special слов** | ingest (Python) | Сравнение с глобальным словарем → список слов                              | `tags.spec_words`                    |
| **NER**                   | ingest (natasha)| Извлечение персон, организаций, локаций                                   | передаётся или логируется            |
| **Генерация word cloud**  | server (Node.js)| Частотный анализ + стоп-слова → топ-50 слов                               | `wordCloud`                          |
| **Подсветка слов**        | клиент (JS)     | Поиск точных вхождений слов из `tags.spec_words` в тексте                 | визуальная анимация                  |

## 🔗 Клиент ↔ Сервер взаимодействие

### Основной поток событий

1. **Ingest-слой** (Python) получает сообщение из Telegram  
2. Скачивает медиа (если есть) → подписывает событие → сохраняет в базу  
3. База данных отправляет уведомление (LISTEN/NOTIFY)  
4. **Сервер** (Node.js) получает уведомление  
5. Проверяет подпись, генерирует облако слов, синхронизирует медиа-файлы  
6. Формирует payload и рассылает всем подключённым клиентам через WebSocket
* при старте используется демо-контент;
* при отсутствии данных — fallback-режим;
* при восстановлении соединения — плавная синхронизация.
### Форматы данных WebSocket

#### Сообщение
- текст  
- дата  
- облако слов (массив {word, freq})  
- метка приоритета (если есть)

#### Новое медиа
- id файла  
- тип файла  
- время изменения  
- метка приоритета (если есть)

## 📁 Обработка медиа

### Ingest

* удаление EXIF
* перекодирование видео (H.264 + AAC)
* UUID-файлы
* хранение метаданных в БД

### Client

* уведомление через WebSocket
* приоритетная очередь
* fallback на HTTP polling

### Rotation

* active / archive директории
* лимиты на количество файлов


## 🛡️ Reliability & Backpressure

* throttling на уровне consumer
* контроль размера WS-буфера
* heartbeat + auto-reconnect
* graceful skipping при перегрузке

Система ориентирована на **стабильную визуализацию**, а не на lossless processing.


## 🔧 Инженерные решения

| Aspect       | Decision                 | Trade-off                 |
| ------------ | ------------------------ | ------------------------- |
| Event broker | PostgreSQL LISTEN/NOTIFY | Простота vs масштаб       |
| Storage      | JSONB + partitions       | Гибкость vs строгая схема |
| Enrichment   | Async worker             | Eventual consistency      |
| Transport    | WebSocket                | Low latency               |
| Frontend     | Vanilla JS               | Минимум зависимостей      |


## 📚 Tech Stack

**Backend**

* Python 3.11–3.12 (asyncio, Telethon, asyncpg)
* Node.js (ws, express, pg-listen)

**Frontend**

* Vanilla JS
* WebSocket
* requestAnimationFrame
* Typewriter animations
* Word Cloud

**Database**

* PostgreSQL 16+ (partitions, JSONB, LISTEN/NOTIFY)

## 📁 Структура проекта

```
index.html
├── HEAD
│   ├── Мета-теги Open Graph/Twitter
│   ├── Inline SVG фавикон
│   └── Inline CSS стили
├── BODY
│   ├── Flash overlay (анимации)
│   ├── Media slides (слайд-шоу)
│   ├── Content area
│   │   ├── Текстовый дисплей
│   │   ├── Canvas для облака слов
│   │   └── Панель управления
│   └── JavaScript модули
└── Inline WordCloud2.js
```



## ⚙️ Структура

### Основные классы

#### 1. `Typewriter`
Анимированный вывод текста с подсветкой

```javascript
const fxText = new Typewriter(element);
await fxText.setText("Текст", highlights, prefix, speed);
```

#### 2. `MediaSlides`
Управление медиа-слайдами

```javascript
const mediaSlides = new MediaSlides(messageDisplay);
await mediaSlides.init(); // Инициализация
await mediaSlides.nextSlide(); // Следующий слайд
```

#### 3. `MessageDisplay`
Основной контроллер отображения

```javascript
const display = new MessageDisplay();
display.initWebSocket(); // Подключение WebSocket
display.loadLocalPhrases(); // Загрузка локальных фраз
```

### Поток данных

```
WebSocket → MessageDisplay → Typewriter/WordCloud
API → MediaSlides → DOM Elements
Локальные фразы → Последовательный показ → Основной режим
```

## 🎨 Интерфейс

### Структура DOM

```html
<div class="container">
    <div class="flash"></div>                <!-- Анимация вспышки -->
    <div class="media-slides"></div>         <!-- Медиа слайды -->
    <div class="content">
        <div class="text"></div>            <!-- Текстовый дисплей -->
        <canvas id="wordCloud"></canvas>    <!-- Облако слов -->
        <div class="controls">
            <button>AUTO (0)</button>       <!-- Пауза/автопрокрутка -->
            <button>NEXT</button>           <!-- Следующий слайд -->
            <button>RESET (0)</button>      <!-- Сброс очереди -->
        </div>
    </div>
</div>
```

### CSS Переменные

```css
:root {
    --bg-color: #212121;                    /* Основной фон */
    --text-color: #bdbdbd;                  /* Цвет текста */
    --highlight-color: #0aeb51;             /* Цвет подсветки */
    --progress-bar-color: #FF5555;          /* Цвет прогресс-бара */
    --indicator-green: #05f10f;             /* Зеленый индикатор */
    --indicator-red: #f52323;               /* Красный индикатор */
}
```

## 🔌 API и WebSocket

### WebSocket подключение

```javascript
ws://0808.us.nekhebet.su:8080
```

### Формат сообщений

```json
{
    "type": "new_media",
    "id": 12345,
    "file_type": "jpg",
    "special_id": 1
}

{
    "text": "Сообщение",
    "tags": {"new_words": ["ключевые", "слова"]},
    "wordCloud": [{"word": "WORD", "freq": 5}],
    "special_id": "1b"
}
```

### REST Endpoints

```javascript
GET  /media-files          // Список медиа файлов
GET  /new-media-files      // Новые медиа файлы
GET  /local.json           // Локальные фразы
GET  /media/{id}           // Получение медиа файла
```

## 📱 Адаптивный дизайн

### Брейкпоинты

```css
@media (max-height: 600px) {
    /* Компактный режим для низких экранов */
}

@media (orientation: landscape) {
    /* Ландшафтный режим */
}

@media (max-width: 600px) {
    /* Мобильные устройства */
}

@media (hover: none) and (pointer: coarse) {
    /* Сенсорные устройства */
}
```

### Особенности для мобильных устройств

- Увеличенные области касания (min-height: 44px)
- Отключение hover-эффектов
- Упрощенные анимации
- Сенсорные жесты

## 🔒 Безопасность

### Меры защиты

1. **Санкцияция текста**
   ```javascript
   sanitizeText(text) {
       return text.replace(/[<>]/g, '').slice(0, 500);
   }
   ```

2. **Проверка DOM элементов**
   - Проверка существования элементов перед использованием
   - Graceful degradation при ошибках

3. **WebSocket защита**
   - Автоматическое переподключение при разрыве
   - Обработка ошибок соединения

## 📊 Производительность

### Оптимизации

1. **Предзагрузка медиа**
   - Lazy loading для неактивных слайдов
   - Eager loading для следующих слайдов

2. **Анимации**
   - `will-change` для трансформаций
   - CSS transitions вместо JavaScript
   - Hardware acceleration через `transform: translateZ(0)`

3. **Память**
   - Ограничение очереди сообщений (100 элементов)
   - Очистка удаленных слайдов из DOM


## 🎯 Режимы работы

### 1. Локальный режим (запуск)
- Отображение локальных фраз из `local.json`
- Последовательный показ всех фраз
- Автоматический переход в нормальный режим

### 2. Нормальный режим
- Отображение медиа из API
- Обработка WebSocket сообщений
- Интерактивное облако слов

### 3. Приоритетный режим
- Специальные сообщения (`special_id`)
- Мгновенное переключение на приоритетный контент
- Двойная вспышка экрана для уведомления

## ⚙️ Конфигурация

### Параметры MessageDisplay

```javascript
MAX_QUEUE_SIZE = 100;              // Макс. размер очереди
MESSAGE_DISPLAY_DURATION = 6000;   // Длительность показа сообщения (мс)
LOCAL_PHRASE_DURATION = 5000;      // Длительность локальной фразы (мс)
MIN_MESSAGE_INTERVAL = 3000;       // Минимальный интервал между сообщениями
```

### Параметры MediaSlides

```javascript
MAX_FILES = 50;                    // Макс. количество файлов
SWIPE_THRESHOLD = 50;              // Порог свайпа (пиксели)
```

## 🚨 Аварийные сценарии

### Потеря соединения WebSocket
1. Автоматическая попытка переподключения через 6 секунд
2. Отображение локального контента
3. Сохранение сообщений в буфер

### Ошибки загрузки медиа
1. Повторная попытка (до 3 раз)
2. Пропуск поврежденного файла
3. Отображение сообщения об ошибке

### Нехватка памяти
1. Ограничение количества слайдов (50)
2. Очистка старых сообщений из очереди
3. Удаление неактивных слайдов из DOM

## 📖 Глоссарий

| Термин | Определение |
|--------|-------------|
| **WordCloud** | Интерактивное облако слов с анимацией |
| **Typewriter** | Эффект печатной машинки для текста |
| **MediaSlide** | Отдельный слайд с медиа-контентом |
| **Special ID** | Идентификатор приоритетного контента |
| **Local Phrases** | Локальные фразы для начального показа |

## 📊 Статус

Рабочий прототип конвейера событий.  
Большая часть кода не публикуется.  
Открытая часть — структура, ключевые паттерны, обработка ошибок, backpressure, клиент.

## 📄 Лицензия

MIT License — полный текст в файле LICENSE.

